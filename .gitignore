node_modules/
dist/
*.log
.DS_Store
test-results/
playwright-report/

# Native build outputs — the projects are committed, their artifacts are not.
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
ios/App/build/
ios/App/Pods/
ios/App/App/public/
ios/App/Podfile.lock
*.jks
*.keystore
*.p12
*.mobileprovision

# The shareable single file is a build artifact of `npm run shareable`, but it
# is COMMITTED on purpose: it is the download the app links to. Left tracked.
